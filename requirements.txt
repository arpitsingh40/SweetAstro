# SweetAstro runtime dependencies
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
httpx>=0.27
python-dotenv>=1.0

# Authoritative ephemeris (required for production chart accuracy).
# Without it the engine falls back to an approximate built-in model.
pyswisseph>=2.10.3

# Development / verification
pytest>=8.0
pytest-timeout>=2.3
